# Caminhos que casam o padrão de chave do check-secrets e NÃO são segredo (DA-240 do servidor de origem).
# Formato: caminho | motivo | AAAA-MM-DD — linha sem motivo ou sem data é IGNORADA (o check avisa).
# Só o caminho listado é isento (árvore + histórico, porta 2). Arquivo novo nunca está aqui; um segredo
# real que entre num arquivo listado passaria — por isso a lista é de fixtures, curta e revisada.
tests/test_claude_cli_engine.py | cabeçalho Authorization com token de TESTE ("Bearer test-…") na fixture do motor claude-cli, desde 54332a8 — sem valor real | 2026-09-06
